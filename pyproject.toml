[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sqlpilot"
version = "0.1.0"
description = "A production-grade Text-to-SQL engine with clarification and self-correction."
requires-python = ">=3.11"
